<div class="page-header">
  <h1>Olá <%= current_user.name %>! <small>Esse é sua dashboard</small></h1>
</div>

<div class="row">
  <div class="col-lg-12">
    <div class="well">
    <% if current_user.teachers.empty? %>
      Você não possui nenhum professor associado a sua conta. <%= link_to 'Procurar professores', enrollments_path %>
    <% else %>
      <ul>
      <% current_user.teachers.each do |teacher| %>
        <li><%= link_to teacher.name, teacher %></li>
      <% end %>
      </ul>
    <% end %>
    </div>
  </div>

  <div class="col-lg-12">
    <div class="well">
      Você não possui treinos para execução. <%= link_to 'Procurar treinos', '#' %>
    </div>
  </div>

  <div class="col-lg-12">
    <%= link_to "Enviar relatório de execução de treino", '#', class: 'btn btn-default btn-lg btn-info btn-block' %>
  </div>

  <div class="col-lg-12">
    <br>
    <%= link_to "Enviar formulário de PAR-Q", new_form_parq_path, class: 'btn btn-default btn-lg btn-info btn-block' %>
  </div>


</div>
